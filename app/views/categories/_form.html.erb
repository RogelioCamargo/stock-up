<div class="mt-n2 w-50 d-none d-lg-block">
	<%= render "shared/errors" %>
	<form action="<%= categories_url %>" method="post">
		<%= authenticity_token %>
		<div class="d-flex">
			<input class="form-control form-control-sm" type="text" name="category[name]" id="category_name">
			<button class="btn btn-primary btn-sm w-7-rem ms-1" type="submit">Create</button>
		</div>
	</form>
</div>
<div class="mt-n2 d-lg-none">
	<%= render "shared/errors" %>
	<form action="<%= categories_url %>" method="post">
		<%= authenticity_token %>
		<div class="d-flex">
			<input class="form-control form-control-sm" type="text" name="category[name]" id="category_name">
			<button class="btn btn-primary btn-sm w-7-rem ms-1" type="submit">Create</button>
		</div>
	</form>
</div>